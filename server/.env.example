# Local dev (SQLite, zero setup):
DATABASE_URL="file:./dev.db"

# Production (PostgreSQL): set provider="postgresql" in prisma/schema.prisma, then:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/stayeasy?schema=public"

PORT=8787

# Comma-separated CORS allowlist for production (omit for open/local dev):
# CORS_ORIGIN="https://bstars00-rgb.github.io"

{
  "name": "stayeasy-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "StayEasy Vietnam backend scaffold — implements src/api/contract.ts (in-memory, reuses the shared catalogue + search engine).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
